#include "main.h"
/**
 * _abs - compute absolute value of an integer
 * @r: the value
 * Description: the absolute value for 0
 * Return: 0
 */
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (r * -1);
}
}
